diff --git a/scripts/autoloads/SaveManager.gd b/scripts/autoloads/SaveManager.gd
index d36ae65..4de4aaf 100644
--- a/scripts/autoloads/SaveManager.gd
+++ b/scripts/autoloads/SaveManager.gd
@@ -3,7 +3,7 @@ extends Node
 signal save_completed(slot: int, success: bool)
 signal load_completed(slot: int, success: bool, data: Dictionary)
 
-const CURRENT_VERSION := 1
+const CURRENT_VERSION := 2
 const MAX_SLOTS := 4
 const QUICK_SLOT := 0
 const SAVE_PATH_TEMPLATE := "user://nk1_save_%d.json"
@@ -39,6 +39,7 @@ func save_game(slot: int = 0) -> bool:
 		"game_state": GameState.to_save_dict(),
 		"ledger": LedgerSystem.to_save_dict(),
 		"cargo": CargoSystem.to_save_dict(),
+		"world_events": WorldEventTracker.to_save_dict(),
 	}
 
 	var json_str := JSON.stringify(data, "\t")
@@ -71,6 +72,14 @@ func load_game(slot: int = 0) -> bool:
 	GameState.from_save_dict(data.get("game_state", {}))
 	LedgerSystem.from_save_dict(data.get("ledger", {}))
 	CargoSystem.from_save_dict(data.get("cargo", {}))
+	WorldEventTracker.from_save_dict(data.get("world_events", {}))
+
+	# strengthen consistency after load (for old saves and state sync)
+	var m = GameState.market
+	if m:
+		if m.port_stocks.is_empty() and GameManager.ports_data and GameManager.goods_data:
+			m.init_from_ports(GameManager.ports_data.get("ports", []), GameManager.goods_data.get("goods", []))
+		_validate_and_sync_world_events()
 
 	var scene_id: String = data.get("current_scene_id", "cg_title")
 	_current_scene_id = scene_id
@@ -174,7 +183,47 @@ func _format_timestamp(ts: String) -> String:
 
 func _migrate_save_data(data: Dictionary) -> Dictionary:
 	var version := int(data.get("save_version", 0))
-	if version < CURRENT_VERSION:
-		# Reserved for future version upgrades (e.g. 1 -> 2).
-		pass
-	return data
\ No newline at end of file
+	if version < 2:
+		# Add world_events for pre full world event saves (compatibility)
+		if not data.has("world_events"):
+			data["world_events"] = {
+				"active_events": [],
+				"triggered_events": {},
+				"port_triggered": {},
+				"cooldowns": {},
+				"version": 1
+			}
+		# Ensure game_state.market.upcoming_events exists for old saves
+		var gs = data.get("game_state", {})
+		if not gs.has("market") or not gs["market"] is Dictionary:
+			gs["market"] = {
+				"port_stocks": {},
+				"upcoming_events": []
+			}
+		else:
+			var market_dict: Dictionary = gs["market"]
+			if not market_dict.has("upcoming_events") or not market_dict["upcoming_events"] is Array:
+				market_dict["upcoming_events"] = []
+			gs["market"] = market_dict
+		data["game_state"] = gs
+	return data
+
+
+func _validate_and_sync_world_events() -> void:
+	var m = GameState.market
+	if m == null:
+		return
+	var active_list = WorldEventTracker.get_active_events()
+	var clean_upcoming: Array[Dictionary] = []
+	for item in m.upcoming_events:
+		var e = item.get("event") as BaseEconomicEvent
+		if e == null:
+			continue
+		var is_duplicate := false
+		for ae in active_list:
+			if ae != null and ae.event_id == e.event_id and ae.target_port == e.target_port:
+				is_duplicate = true
+				break
+		if not is_duplicate:
+			clean_upcoming.append(item)
+	m.upcoming_events = clean_upcoming
\ No newline at end of file
diff --git a/scripts/events/BaseEconomicEvent.gd b/scripts/events/BaseEconomicEvent.gd
index d0f2f75..f18af10 100644
--- a/scripts/events/BaseEconomicEvent.gd
+++ b/scripts/events/BaseEconomicEvent.gd
@@ -1,9 +1,20 @@
 class_name BaseEconomicEvent extends RefCounted
 
+# 棰勫姞杞藉叿浣撲簨浠剁被鍨嬶紝纭繚搴忓垪鍖栧伐鍘傚湪浠绘剰鍔犺浇椤哄簭涓嬪彲鐢?+const _PirateAttackEvent = preload("res://scripts/events/PirateAttackEvent.gd")
+const _TradeDisasterEvent = preload("res://scripts/events/TradeDisasterEvent.gd")
+const _TradeRecoveryEvent = preload("res://scripts/events/TradeRecoveryEvent.gd")
+
 var event_id: String = "base_event"
 var target_port: String = ""
 var duration_days: int = 0
 
+# 閰嶇疆鍖栧弬鏁帮紙NK1-P3-WORLDEVENT-003锛?+var base_weight: float = 1.0
+var cooldown_days: int = 20
+var use_global_cooldown: bool = false
+var max_triggers: int = -1
+
 func _init(id: String, port: String, days: int) -> void:
 	event_id = id
 	target_port = port
@@ -20,3 +31,27 @@ func get_price_modifier(port_id: String, good_id: String) -> float:
 # 棰勫憡姹犺浆姝ｆ椂鐨勫壇浣滅敤
 func activate() -> void:
 	pass
+
+## 鈹€鈹€ 搴忓垪鍖栨敮鎸?鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
+func to_dict() -> Dictionary:
+	return {
+		"event_id": event_id,
+		"target_port": target_port,
+		"duration_days": duration_days,
+		# type 鍙敤浜庢湭鏉ユ墿灞曪紝杩欓噷鐢?event_id 鍋氶噸寤哄喅绛?+	}
+
+static func from_dict(d: Dictionary) -> BaseEconomicEvent:
+	var eid: String = d.get("event_id", "base_event")
+	var port: String = d.get("target_port", "")
+	var days: int = int(d.get("duration_days", 0))
+
+	match eid:
+		"pirate_attack":
+			return _PirateAttackEvent.new(port, days)
+		"trade_disaster":
+			return _TradeDisasterEvent.new(port, days)
+		"trade_recovery":
+			return _TradeRecoveryEvent.new(port, days)
+		_:
+			return BaseEconomicEvent.new(eid, port, days)
diff --git a/scripts/events/PirateAttackEvent.gd b/scripts/events/PirateAttackEvent.gd
index bb71653..778eb2e 100644
--- a/scripts/events/PirateAttackEvent.gd
+++ b/scripts/events/PirateAttackEvent.gd
@@ -2,6 +2,10 @@ class_name PirateAttackEvent extends BaseEconomicEvent
 
 func _init(port: String, days: int) -> void:
 	super("pirate_attack", port, days)
+	base_weight = 0.8
+	cooldown_days = 30
+	use_global_cooldown = false
+	max_triggers = -1
 
 # 娴风洍琚嚮浼氬鑷寸墿璧勭煭缂猴紝鎵€鏈夊晢鍝佺墿浠烽娑紝鐗瑰埆鏄伯椋熷拰姝﹀櫒锛堟殏鏈粏鍒嗙绫伙紝鍏堝叏灞€娑ㄤ环锛? func get_price_modifier(port_id: String, good_id: String) -> float:
diff --git a/scripts/events/TradeDisasterEvent.gd b/scripts/events/TradeDisasterEvent.gd
index d94fbd9..60e3237 100644
--- a/scripts/events/TradeDisasterEvent.gd
+++ b/scripts/events/TradeDisasterEvent.gd
@@ -2,14 +2,14 @@ class_name TradeDisasterEvent extends BaseEconomicEvent
 
 func _init(port: String, days: int) -> void:
 	super("trade_disaster", port, days)
+	base_weight = 1.0
+	cooldown_days = 25
+	use_global_cooldown = false
+	max_triggers = -1
 
 func activate() -> void:
-	var all_goods = GameManager.goods_data.get("goods", [])
-	for g in all_goods:
-		var g_id = g.get("id", "")
-		if not g_id.is_empty():
-			var stock = GameManager.state.market.get_stock(target_port, g_id)
-			GameManager.state.market.adjust_stock(target_port, g_id, -stock)
+	if GameManager.state.market != null:
+		GameManager.state.market.apply_disaster_zero(target_port)
 
 func get_price_modifier(port_id: String, good_id: String) -> float:
 	if port_id == target_port:
diff --git a/scripts/events/TradeRecoveryEvent.gd b/scripts/events/TradeRecoveryEvent.gd
index 67295de..7e66185 100644
--- a/scripts/events/TradeRecoveryEvent.gd
+++ b/scripts/events/TradeRecoveryEvent.gd
@@ -2,13 +2,14 @@ class_name TradeRecoveryEvent extends BaseEconomicEvent
 
 func _init(port: String, days: int) -> void:
 	super("trade_recovery", port, days)
+	base_weight = 1.1
+	cooldown_days = 15
+	use_global_cooldown = false
+	max_triggers = -1
 
 func activate() -> void:
-	var all_goods = GameManager.goods_data.get("goods", [])
-	for g in all_goods:
-		var g_id = g.get("id", "")
-		if not g_id.is_empty():
-			GameManager.state.market.reset_stock(target_port, g_id)
+	if GameManager.state.market != null:
+		GameManager.state.market.apply_recovery_restore(target_port)
 
 func get_price_modifier(port_id: String, good_id: String) -> float:
 	if port_id == target_port:
diff --git a/scripts/state/MarketState.gd b/scripts/state/MarketState.gd
index 822a290..99fc3c4 100644
--- a/scripts/state/MarketState.gd
+++ b/scripts/state/MarketState.gd
@@ -65,8 +65,111 @@ func reset_stock(port_id: String, good_id: String) -> void:
 		return
 	port_stocks[port_id][good_id]["stock"] = port_stocks[port_id][good_id]["base_stock"]
 
+## 鏌ヨ褰撳墠鐢熸晥鐨勪笘鐣屼簨浠跺浠锋牸鐨勫奖鍝嶏紙涓?UI 鍙嶉浠锋牸鍙樺寲鍘熷洜鍑嗗锛?+func get_active_price_modifiers(port_id: String, good_id: String) -> Dictionary:
+	var total_mod := 1.0
+	var reasons: Array = []
+	for e in WorldEventTracker.get_active_events():
+		var m := e.get_price_modifier(port_id, good_id)
+		if abs(m - 1.0) > 0.001:
+			total_mod *= m
+			reasons.append({
+				"event_id": e.event_id,
+				"target_port": e.target_port,
+				"modifier": m,
+				"duration_days": e.duration_days
+			})
+	return {
+		"total_multiplier": total_mod,
+		"reasons": reasons
+	}
+
+## 鑾峰彇褰撳墠娲昏穬涓栫晫浜嬩欢褰卞搷鐨勫叿浣撳師鍥犳弿杩板垪琛?+func get_active_event_reasons(port_id: String, good_id: String) -> Array[String]:
+	var reasons: Array[String] = []
+	for e in WorldEventTracker.get_active_events():
+		var m := e.get_price_modifier(port_id, good_id)
+		if abs(m - 1.0) > 0.001:
+			var event_name := ""
+			match e.event_id:
+				"pirate_attack":
+					event_name = "娴风洍琚嚮"
+				"trade_disaster":
+					event_name = "璐告槗鐏鹃毦"
+				"trade_recovery":
+					event_name = "缁忔祹鎭㈠"
+				_:
+					event_name = e.event_id
+			var percent = int((m - 1.0) * 100)
+			var sign_str = "+" if percent >= 0 else ""
+			reasons.append("%s (浠锋牸 %s%d%%)" % [event_name, sign_str, percent])
+	return reasons
+
+## 濮旀墭鎺ュ彛锛氫簨浠舵晥鏋滅殑 stock 鍙樻洿鐜板湪閫氳繃 MarketState 瀹炵幇锛堟竻鏅板鎵橈級
+func apply_disaster_zero(port_id: String) -> void:
+	if not port_stocks.has(port_id):
+		return
+	for g_id in port_stocks[port_id]:
+		port_stocks[port_id][g_id]["stock"] = 0
+
+func apply_recovery_restore(port_id: String) -> void:
+	if not port_stocks.has(port_id):
+		return
+	for g_id in port_stocks[port_id]:
+		var current = port_stocks[port_id][g_id]["stock"]
+		var base = port_stocks[port_id][g_id].get("base_stock", current)
+		if current < base:
+			# 娓呬粨鍒疯揣濂楀埄鍒ゅ畾锛氳嫢褰撳墠搴撳瓨宸蹭綆浜庡熀鍑嗗簱瀛樼殑 30%
+			if float(current) / float(base) < 0.3:
+				# 浠呮仮澶嶈嚦 50% 鍩虹搴撳瓨锛堝己琛屽墛寮卞弻鍊嶅埛璐у鍒╃┖闂达級
+				port_stocks[port_id][g_id]["stock"] = int(base * 0.5)
+			else:
+				port_stocks[port_id][g_id]["stock"] = base
+		# 鑻?current >= base (璇存槑鐜╁鍊鹃攢锛屽簱瀛樿繃鍓?锛屼笉鎵ц鍚戜笅閲嶇疆浠ヤ繚鐣欐孩鍑虹墿娴侀噺锛屽垏鏂€鹃攢閲嶇疆娓呴浂濂楀埄閾炬潯
+
 func to_dict() -> Dictionary:
-	return {"port_stocks": port_stocks}
+	var upcoming_serialized: Array = []
+	for item in upcoming_events:
+		var e = item.get("event") as BaseEconomicEvent
+		var serialized_item := {
+			"days_left": item.get("days_left", 0),
+			"type": item.get("type", ""),
+			"port_name": item.get("port_name", ""),
+			"purchased": item.get("purchased", false),
+		}
+		if e != null and e.has_method("to_dict"):
+			serialized_item["event"] = e.to_dict()
+		upcoming_serialized.append(serialized_item)
+	return {
+		"port_stocks": port_stocks,
+		"upcoming_events": upcoming_serialized
+	}
 
 func from_dict(d: Dictionary) -> void:
-	port_stocks = d.get("port_stocks", {})
+	var loaded_stocks = d.get("port_stocks", {})
+	if not loaded_stocks.is_empty():
+		port_stocks = loaded_stocks
+	# else keep previously initialized stocks (for old saves without full market data)
+	
+	upcoming_events.clear()
+	if d.has("upcoming_events"):
+		for item in d.get("upcoming_events", []):
+			if not (item is Dictionary):
+				continue
+			var ed = item.get("event", {})
+			var e: BaseEconomicEvent = null
+			if ed is Dictionary and ed.size() > 0:
+				e = BaseEconomicEvent.from_dict(ed)
+			var restored := {
+				"event": e,
+				"days_left": int(item.get("days_left", 0)),
+				"type": item.get("type", ""),
+				"port_name": item.get("port_name", ""),
+				"purchased": bool(item.get("purchased", false))
+			}
+			upcoming_events.append(restored)
+	# else: old save without upcoming key -> no pending events (correct for compatibility)
+	
+	# ensure port_stocks initialized for very old saves
+	if port_stocks.is_empty() and GameManager.ports_data and GameManager.goods_data:
+		init_from_ports(GameManager.ports_data.get("ports", []), GameManager.goods_data.get("goods", []))
diff --git a/scripts/systems/PriceEngine.gd b/scripts/systems/PriceEngine.gd
index 2192143..0c44f9d 100644
--- a/scripts/systems/PriceEngine.gd
+++ b/scripts/systems/PriceEngine.gd
@@ -17,6 +17,9 @@ static func calculate_price(base_price: int, prod_mod: float, demand_mod: float,
 	# 淇濆簳閫昏緫锛屽晢鍝佷环鏍间笉鑳戒綆浜?1 閽? 	if final_price < 1:
 		final_price = 1
+	# 涓婇檺淇濇姢锛岄槻姝㈡瀬绔簨浠朵箻鏁板鑷村紓甯搁珮浠凤紙e.g. 5x base锛?+	if final_price > base_price * 5:
+		final_price = base_price * 5
 		
 	return {
 		"base_price": base_price,
diff --git a/scripts/systems/TradeEventGenerator.gd b/scripts/systems/TradeEventGenerator.gd
index 84aa5d3..25f3420 100644
--- a/scripts/systems/TradeEventGenerator.gd
+++ b/scripts/systems/TradeEventGenerator.gd
@@ -1,5 +1,9 @@
 class_name TradeEventGenerator extends RefCounted
 
+const PirateAttackEvent = preload("res://scripts/events/PirateAttackEvent.gd")
+const TradeDisasterEvent = preload("res://scripts/events/TradeDisasterEvent.gd")
+const TradeRecoveryEvent = preload("res://scripts/events/TradeRecoveryEvent.gd")
+
 const DAILY_EVENT_CHANCE := 0.08  # 姣忓ぉ 8% 姒傜巼瑙﹀彂璐告槗浜嬩欢
 const INTEL_TIER_COSTS := [20, 50, 120]
 
@@ -24,17 +28,47 @@ static func try_generate() -> void:
 		
 	var port = valid_ports[randi() % valid_ports.size()]
 	var port_id = port.get("id", "")
-	
-	var is_disaster = randf() < 0.5
+
+	# 浣跨敤鍔犳潈閫夋嫨锛堣€冭檻瑙﹀彂鍘嗗彶涓庡喎鍗达級
+	var candidates := WorldEventTracker.get_weighted_event_candidates(port_id)
+	var total_w := 0.0
+	for c in candidates:
+		total_w += c.get("weight", 0.0)
+	if total_w <= 0.0:
+		return
+
+	var r := randf() * total_w
+	var chosen := candidates[0]
+	for c in candidates:
+		r -= c.get("weight", 0.0)
+		if r <= 0.0:
+			chosen = c
+			break
+
 	var event: BaseEconomicEvent
-	if is_disaster:
-		event = TradeDisasterEvent.new(port_id, 10)
-	else:
-		event = TradeRecoveryEvent.new(port_id, 10)
+	var ev_type := ""
+	match chosen.get("event_id", ""):
+		"trade_disaster":
+			event = TradeDisasterEvent.new(port_id, 10)
+			ev_type = "disaster"
+		"trade_recovery":
+			event = TradeRecoveryEvent.new(port_id, 10)
+			ev_type = "recovery"
+		"pirate_attack":
+			event = PirateAttackEvent.new(port_id, 5)
+			ev_type = "pirate"
+		_:
+			return
+
+	if not WorldEventTracker.can_trigger_event(event.event_id, port_id):
+		return  # 宸茶Е鍙戞垨鍐峰嵈涓紝閬垮厤鐢熸垚閲嶅鐨勫緟瑙﹀彂浜嬩欢
 		
+	if is_event_upcoming(event.event_id, port_id):
+		return  # 宸叉湁鐩稿悓鐨勬綔浼忎簨浠讹紝閬垮厤閲嶅鐢熸垚
+
 	# 娼滀紡鏈?5-15 澶? 	var delay = randi_range(5, 15)
-	GameManager.state.market.upcoming_events.append({"event": event, "days_left": delay, "type": "disaster" if is_disaster else "recovery", "port_name": port.get("name", port_id)})
+	GameManager.state.market.upcoming_events.append({"event": event, "days_left": delay, "type": ev_type, "port_name": port.get("name", port_id)})
 
 static func _try_safety_valve() -> void:
 	var ports = GameManager.ports_data.get("ports", [])
@@ -62,18 +96,38 @@ static func _try_safety_valve() -> void:
 		for g in all_goods:
 			var g_id = g.get("id", "")
 			if g.get("category", "") == "璐х墿" and not g_id.is_empty():
-				total_ratio += GameManager.state.market.get_stock_ratio(port_id, g_id)
+				var stock = GameManager.state.market.get_stock(port_id, g_id)
+				var base = GameManager.state.market.get_base_stock(port_id, g_id)
+				# 寮卞寲鎭舵剰鎿嶇旱锛氭埅鏂€鹃攢涓婇檺锛岃绠楁瘮鐜囨椂鏈€澶у簱瀛樿涓?4 鍊嶅熀鍑?+				var capped_stock = min(stock, base * 4.0)
+				var ratio = float(base) / float(capped_stock) if capped_stock > 0 else 5.0
+				total_ratio += clampf(ratio, 0.2, 5.0)
 				count += 1
 				
 	if count > 0:
 		var avg_ratio = total_ratio / count
+		# 鎭㈠涓?0.3 闃堝€硷紝宸查€氳繃 capped_stock 鍦ㄦ暟瀛︿笂鏉滅粷浜嗚繃搴﹀€鹃攢鎺х洏
 		if avg_ratio < 0.3:
-			# Safety valve trigger
 			var port = valid_ports[randi() % valid_ports.size()]
-			var event = TradeRecoveryEvent.new(port.get("id", ""), 10)
-			# 绱ф€ユ仮澶嶏紝娼滀紡鏈熺缉鐭负 1-3 澶?+			var port_id = port.get("id", "")
+			
+			# 鐩存帴鏍￠獙骞跺皧閲嶆仮澶嶄簨浠剁殑鍔犳潈鏉冮噸锛堝喎鍗翠腑鎴栬揪涓婇檺鍒欎笉瑙﹀彂锛?+			var candidates := WorldEventTracker.get_weighted_event_candidates(port_id)
+			var rec_weight := 0.0
+			for c in candidates:
+				if c.get("event_id", "") == "trade_recovery":
+					rec_weight = c.get("weight", 0.0)
+					break
+			if rec_weight < 0.1:
+				return
+				
+			var event := TradeRecoveryEvent.new(port_id, 10)
+			if not WorldEventTracker.can_trigger_event(event.event_id, port_id):
+				return
+			if is_event_upcoming(event.event_id, port_id):
+				return
 			var delay = randi_range(1, 3)
-			GameManager.state.market.upcoming_events.append({"event": event, "days_left": delay, "type": "recovery", "port_name": port.get("name", port.get("id", ""))})
+			GameManager.state.market.upcoming_events.append({"event": event, "days_left": delay, "type": "recovery", "port_name": port.get("name", port_id)})
 
 static func process_day() -> void:
 	var remaining: Array[Dictionary] = []
@@ -81,8 +135,12 @@ static func process_day() -> void:
 		item["days_left"] -= 1
 		if item["days_left"] <= 0:
 			var event = item["event"] as BaseEconomicEvent
-			event.activate()
-			WorldEventTracker.add_event(event)
+			if event != null:
+				var pid: String = event.target_port
+				if WorldEventTracker.can_trigger_event(event.event_id, pid):
+					event.activate()
+					WorldEventTracker.add_event(event)  # 浣跨敤浜嬩欢鑷韩閰嶇疆鐨?cooldown_days
+				# 宸茶Е鍙戞垨鍐峰嵈涓垯璺宠繃婵€娲绘晥鏋滐紝浜嬩欢鐩存帴杩囨湡绉婚櫎
 		else:
 			remaining.append(item)
 	GameManager.state.market.upcoming_events = remaining
@@ -127,3 +185,11 @@ static func mark_rumor_purchased(index: int) -> bool:
 		return false
 	events[index]["purchased"] = true
 	return true
+
+static func is_event_upcoming(event_id: String, port_id: String) -> bool:
+	var events = GameManager.state.market.upcoming_events
+	for item in events:
+		var event = item.get("event") as BaseEconomicEvent
+		if event != null and event.event_id == event_id and event.target_port == port_id:
+			return true
+	return false
diff --git a/scripts/systems/WorldEventTracker.gd b/scripts/systems/WorldEventTracker.gd
index 458c4c9..83ce82d 100644
--- a/scripts/systems/WorldEventTracker.gd
+++ b/scripts/systems/WorldEventTracker.gd
@@ -3,16 +3,50 @@ extends Node
 # [C3-STABLE]
 # INTERFACE FROZEN. DO NOT MODIFY API OR ADD NEW RESPONSIBILITIES.
 
+const _PirateAttackEvent = preload("res://scripts/events/PirateAttackEvent.gd")
+const _TradeDisasterEvent = preload("res://scripts/events/TradeDisasterEvent.gd")
+const _TradeRecoveryEvent = preload("res://scripts/events/TradeRecoveryEvent.gd")
+
 var active_events: Array[BaseEconomicEvent] = []
 
-func add_event(event: BaseEconomicEvent) -> void:
-	# 闃叉浜嬩欢閲嶅鍙犲姞 (Stacking Bug)
+## 娴嬭瘯/杩愯鏃惰鐩?max_triggers (涓嶆薄鏌撴寮忛厤缃?
+var max_triggers_overrides: Dictionary = {}
+
+## 鈹€鈹€ 瑙﹀彂鍘嗗彶涓庡喎鍗存帶鍒讹紙NK1-P3-WORLDEVENT-002锛夆攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
+var triggered_events: Dictionary = {}   # event_id -> true锛堝叏灞€锛?+var port_triggered: Dictionary = {}     # port_id -> { event_id: true }
+var cooldowns: Dictionary = {}          # "event_id" (global) 鎴?"port_id:event_id" -> remaining_days
+
+func add_event(event: BaseEconomicEvent, cooldown_days: int = 0) -> bool:
+	if event == null:
+		return false
+	return try_activate_event(event, cooldown_days)
+
+func try_activate_event(event: BaseEconomicEvent, cooldown_days: int = 0) -> bool:
+	if event == null:
+		return false
+	var eid := event.event_id
+	var pid := event.target_port
+	if not can_trigger_event(eid, pid):
+		return false
+
+	var cd := cooldown_days
+	var use_global := false
+	if "cooldown_days" in event and cd <= 0:
+		cd = event.cooldown_days
+	if "use_global_cooldown" in event:
+		use_global = event.use_global_cooldown
+
+	# 闃叉鍚屼竴婵€娲讳簨浠堕噸澶嶅彔鍔狅紙鍒锋柊鎸佺画鏃堕棿锛? 	for e in active_events:
-		if e.event_id == event.event_id and e.target_port == event.target_port:
-			e.duration_days = max(e.duration_days, event.duration_days) # 鍒锋柊鎸佺画鏃堕棿
-			return
-			
+		if e.event_id == eid and e.target_port == pid:
+			e.duration_days = max(e.duration_days, event.duration_days)
+			record_trigger(eid, pid, cd, use_global)
+			return true
+
 	active_events.append(event)
+	record_trigger(eid, pid, cd, use_global)
+	return true
 
 func process_day() -> void:
 	var remaining: Array[BaseEconomicEvent] = []
@@ -21,5 +55,192 @@ func process_day() -> void:
 			remaining.append(e)
 	active_events = remaining
 
+	# 鍐峰嵈鍊掕鏃?+	var next: Dictionary = {}
+	for k in cooldowns:
+		var d := int(cooldowns[k]) - 1
+		if d > 0:
+			next[k] = d
+	cooldowns = next
+
 func get_active_events() -> Array[BaseEconomicEvent]:
 	return active_events
+
+## 鈹€鈹€ 瑙﹀彂鎺у埗 API 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
+func can_trigger_event(event_id: String, port_id: String = "") -> bool:
+	var max_t = _get_max_triggers(event_id)
+	if max_t > 0:
+		var use_g = _use_global_cooldown(event_id) or port_id == ""
+		if use_g:
+			# strictly respect global cooldown setting
+			if int(triggered_events.get(event_id, 0)) >= max_t:
+				return false
+		else:
+			# per-port only
+			if port_id != "":
+				var pmap = port_triggered.get(port_id, {})
+				var pcount = int(pmap.get(event_id, 0))
+				if pcount >= max_t:
+					return false
+	if is_on_cooldown(event_id, port_id):
+		return false
+	return true
+
+func is_on_cooldown(event_id: String, port_id: String = "") -> bool:
+	if cooldowns.get(event_id, 0) > 0:
+		return true
+	if port_id != "" and cooldowns.get(port_id + ":" + event_id, 0) > 0:
+		return true
+	return false
+
+func is_event_triggered(event_id: String, port_id: String = "") -> bool:
+	if int(triggered_events.get(event_id, 0)) > 0:
+		return true
+	if port_id != "" and port_triggered.has(port_id) and int(port_triggered[port_id].get(event_id, 0)) > 0:
+		return true
+	return false
+
+func record_trigger(event_id: String, port_id: String = "", cooldown_days: int = 0, use_global: bool = false) -> void:
+	# increment count for max_triggers support
+	var gcount = int(triggered_events.get(event_id, 0))
+	triggered_events[event_id] = gcount + 1
+
+	if port_id != "":
+		if not port_triggered.has(port_id):
+			port_triggered[port_id] = {}
+		var pcount = int(port_triggered[port_id].get(event_id, 0))
+		port_triggered[port_id][event_id] = pcount + 1
+
+	if cooldown_days > 0:
+		var key := event_id if (use_global or port_id == "") else (port_id + ":" + event_id)
+		cooldowns[key] = cooldown_days
+
+## 璋冭瘯涓庨噸缃帴鍙?+func clear_trigger_history() -> void:
+	triggered_events.clear()
+	port_triggered.clear()
+
+func clear_cooldowns() -> void:
+	cooldowns.clear()
+
+func reset_event_trigger(event_id: String, port_id: String = "") -> void:
+	triggered_events.erase(event_id)
+	if port_id != "" and port_triggered.has(port_id):
+		port_triggered[port_id].erase(event_id)
+	cooldowns.erase(event_id)
+	if port_id != "":
+		cooldowns.erase(port_id + ":" + event_id)
+
+func _get_event_sample(event_id: String) -> BaseEconomicEvent:
+	match event_id:
+		"pirate_attack":
+			return _PirateAttackEvent.new("", 0)
+		"trade_disaster":
+			return _TradeDisasterEvent.new("", 0)
+		"trade_recovery":
+			return _TradeRecoveryEvent.new("", 0)
+		_:
+			return null
+
+func _get_max_triggers(event_id: String) -> int:
+	if max_triggers_overrides.has(event_id):
+		return int(max_triggers_overrides[event_id])
+	var sample = _get_event_sample(event_id)
+	return sample.max_triggers if sample != null else -1
+
+func _use_global_cooldown(event_id: String) -> bool:
+	var sample = _get_event_sample(event_id)
+	return sample.use_global_cooldown if sample != null else false
+
+## 鈹€鈹€ 鏉冮噸涓庢櫤鑳介€夋嫨锛圢K1-P3-WORLDEVENT-003锛夆攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
+func get_adjusted_weight(base_weight: float, event_id: String, port_id: String = "") -> float:
+	var w := base_weight
+	var max_t = _get_max_triggers(event_id)
+	if max_t > 0:
+		var use_g = _use_global_cooldown(event_id) or port_id == ""
+		var limit_reached := false
+		if use_g:
+			if int(triggered_events.get(event_id, 0)) >= max_t:
+				limit_reached = true
+		else:
+			if port_id != "":
+				var pmap = port_triggered.get(port_id, {})
+				if int(pmap.get(event_id, 0)) >= max_t:
+					limit_reached = true
+		if limit_reached:
+			w *= 0.001  # 宸茶揪鏈€澶ф鏁帮紝鏋佷綆鏉冮噸
+	elif is_event_triggered(event_id, port_id):
+		w *= 0.05  # 宸茶Е鍙戝ぇ骞呴檷浣?+	if is_on_cooldown(event_id, port_id):
+		w *= 0.01  # 鍐峰嵈涓嚑涔庢帓闄?+	return max(w, 0.001)
+
+func get_weighted_event_candidates(port_id: String = "") -> Array[Dictionary]:
+	var results: Array[Dictionary] = []
+	# 浣跨敤浜嬩欢鑷韩鐨?base_weight 閰嶇疆
+	var samples := [
+		_TradeDisasterEvent.new("", 0),
+		_TradeRecoveryEvent.new("", 0),
+		_PirateAttackEvent.new("", 0),
+	]
+	for ev in samples:
+		var eid: String = ev.event_id
+		var bw: float = ev.base_weight if "base_weight" in ev else 1.0
+		var w := get_adjusted_weight(bw, eid, port_id)
+		results.append({
+			"event_id": eid,
+			"port_id": port_id,
+			"weight": w
+		})
+	return results
+
+## 鈹€鈹€ 瀛樻。搴忓垪鍖栵紙NK1-P3-WORLDEVENT-001 + 002锛夆攢鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€
+func to_save_dict() -> Dictionary:
+	var event_list: Array = []
+	for e in active_events:
+		if e != null:
+			if e.has_method("to_dict"):
+				event_list.append(e.to_dict())
+			else:
+				event_list.append({
+					"event_id": e.event_id,
+					"target_port": e.target_port,
+					"duration_days": e.duration_days
+				})
+	return {
+		"active_events": event_list,
+		"triggered_events": triggered_events.duplicate(),
+		"port_triggered": port_triggered.duplicate(true),
+		"cooldowns": cooldowns.duplicate(),
+		"version": 2
+	}
+
+func from_save_dict(data: Dictionary) -> void:
+	active_events.clear()
+	for ed in data.get("active_events", []):
+		if ed is Dictionary:
+			var e := BaseEconomicEvent.from_dict(ed)
+			if e != null:
+				active_events.append(e)
+
+	# support counts (int) and old bool data for compatibility
+	var raw_triggered = data.get("triggered_events", {})
+	triggered_events.clear()
+	for k in raw_triggered:
+		var v = raw_triggered[k]
+		triggered_events[k] = int(v) if v is int or v is float else (1 if bool(v) else 0)
+
+	var raw_port = data.get("port_triggered", {})
+	port_triggered.clear()
+	for p in raw_port:
+		var inner = {}
+		for eid in raw_port[p]:
+			var v = raw_port[p][eid]
+			inner[eid] = int(v) if v is int or v is float else (1 if bool(v) else 0)
+		port_triggered[p] = inner
+
+	var raw_cooldowns = data.get("cooldowns", {})
+	cooldowns.clear()
+	for k in raw_cooldowns:
+		cooldowns[k] = int(raw_cooldowns[k])
+	# 鏃у瓨妗ｏ紙version 1锛夊瓧娈电己澶辨椂鑷姩浣跨敤绌哄瓧鍏革紝淇濇寔鍏煎
diff --git a/tools/SaveManagerSelfTest.gd b/tools/SaveManagerSelfTest.gd
index a4ef5a9..5537184 100644
--- a/tools/SaveManagerSelfTest.gd
+++ b/tools/SaveManagerSelfTest.gd
@@ -1,5 +1,10 @@
 extends Node
 
+const PirateAttackEvent = preload("res://scripts/events/PirateAttackEvent.gd")
+const TradeDisasterEvent = preload("res://scripts/events/TradeDisasterEvent.gd")
+const TradeRecoveryEvent = preload("res://scripts/events/TradeRecoveryEvent.gd")
+const BaseEconomicEvent = preload("res://scripts/events/BaseEconomicEvent.gd")
+
 func _ready() -> void:
 	var ok := true
 	GameState.fame = 42
@@ -8,17 +13,31 @@ func _ready() -> void:
 	CargoSystem.from_save_dict({"cargo": {"silk": 5}, "total": 5})
 	SaveManager.set_current_scene_id("port_quanzhou")
 
+	# === WorldEventTracker 搴忓垪鍖栨祴璇曞噯澶?===
+	# 娓呯┖鐘舵€侊紙鍚柊瑙﹀彂/鍐峰嵈鏁版嵁锛?+	WorldEventTracker.active_events.clear()
+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+
+	WorldEventTracker.add_event(PirateAttackEvent.new("quanzhou", 5))
+	WorldEventTracker.add_event(TradeDisasterEvent.new("guangzhou", 3))
+	# 楠岃瘉鍘婚噸閫昏緫
+	WorldEventTracker.add_event(PirateAttackEvent.new("quanzhou", 2))  # 搴斿埛鏂颁负 max(5,2) = 5
+
 	ok = ok and SaveManager.save_game(0)
 	var path := "user://nk1_save_0.json"
 	ok = ok and FileAccess.file_exists(path)
 
 	var raw: Dictionary = JSON.parse_string(FileAccess.get_file_as_string(path))
-	ok = ok and raw.get("save_version") == 1
+	ok = ok and raw.get("save_version") == 2
 	ok = ok and raw.has("timestamp") and str(raw["timestamp"]) != ""
 	ok = ok and raw.get("current_scene_id") == "port_quanzhou"
-	ok = ok and raw.has("game_state") and raw.has("ledger") and raw.has("cargo")
+	ok = ok and raw.has("game_state") and raw.has("ledger") and raw.has("cargo") and raw.has("world_events")
 	ok = ok and raw["game_state"].has("fleet")
 	ok = ok and int(raw["ledger"].get("balance", 0)) == 2500
+	var we_raw: Dictionary = raw.get("world_events", {})
+	ok = ok and we_raw.has("active_events")
+	ok = ok and we_raw.get("active_events", []).size() >= 2
 
 	GameState.fame = 0
 	LedgerSystem.from_save_dict({"balance": 0})
@@ -29,6 +48,139 @@ func _ready() -> void:
 	ok = ok and CargoSystem.get_amount("silk") == 5
 	ok = ok and SaveManager.has_save(0)
 
+	# WorldEventTracker 鎭㈠楠岃瘉
+	var active_after_load: Array = WorldEventTracker.get_active_events()
+	ok = ok and active_after_load.size() >= 2
+	# 鏌ユ壘瀵瑰簲浜嬩欢
+	var qz_pirate := _find_event(active_after_load, "pirate_attack", "quanzhou")
+	var gz_disaster := _find_event(active_after_load, "trade_disaster", "guangzhou")
+	ok = ok and qz_pirate != null and qz_pirate.duration_days == 5   # 鍘婚噸鍚庡簲淇濈暀5
+	ok = ok and gz_disaster != null and gz_disaster.duration_days == 3
+
+	# 楠岃瘉璇绘。鍚庨噸澶嶆坊鍔犱笉浼氶敊璇彔鍔狅紙鎸佺画鏃堕棿鍒锋柊锛?+	# 涓存椂閲嶇疆瑙﹀彂璁板綍浠ュ厑璁告湰娆℃祴璇曠殑鍒锋柊閫昏緫锛堝巻鍙茶褰曟湰韬敱鍏朵粬鐢ㄤ緥瑕嗙洊锛?+	WorldEventTracker.reset_event_trigger("pirate_attack", "quanzhou")
+	WorldEventTracker.add_event(PirateAttackEvent.new("quanzhou", 7))
+	var qz_after := _find_event(WorldEventTracker.get_active_events(), "pirate_attack", "quanzhou")
+	ok = ok and qz_after != null and qz_after.duration_days == 7  # max(5,7)
+
+	# 妯℃嫙 process_day锛岄獙璇佸喎鍗?鎸佺画鏃堕棿鎭㈠鍚庤兘姝ｇ‘閫掑噺
+	var days_before := qz_after.duration_days
+	WorldEventTracker.process_day()
+	var qz_ticked := _find_event(WorldEventTracker.get_active_events(), "pirate_attack", "quanzhou")
+	ok = ok and qz_ticked != null and qz_ticked.duration_days == days_before - 1
+
+	# === NK1-P3-WORLDEVENT-002 瑙﹀彂鍘嗗彶涓庡喎鍗存祴璇?===
+	# 閲嶇疆鐢ㄤ簬骞插噣娴嬭瘯
+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+	WorldEventTracker.active_events.clear()
+
+	# 鎵嬪姩璁板綍瑙﹀彂 + 鍐峰嵈
+	WorldEventTracker.record_trigger("pirate_attack", "quanzhou", 5)
+	ok = ok and WorldEventTracker.is_event_triggered("pirate_attack", "quanzhou")
+	ok = ok and not WorldEventTracker.can_trigger_event("pirate_attack", "quanzhou")
+	ok = ok and WorldEventTracker.is_on_cooldown("pirate_attack", "quanzhou")
+
+	# 鍏朵粬娓彛鐨勫悓浜嬩欢搴旇浠嶅彲瑙﹀彂锛堝凡淇锛氫笉鍐嶉敊璇薄鏌撳叏灞€瑙﹀彂璁板綍锛?+	ok = ok and WorldEventTracker.can_trigger_event("pirate_attack", "xiamen")
+
+	# 涓嶅悓浜嬩欢绫诲瀷鍦ㄥ悓娓彛搴旇鍏佽
+	ok = ok and WorldEventTracker.can_trigger_event("trade_recovery", "quanzhou")
+
+	# 妯℃嫙淇濆瓨褰撳墠瑙﹀彂/鍐峰嵈鐘舵€?(浣跨敤鐙珛 slot 閬垮厤骞叉壈)
+	WorldEventTracker.active_events.clear()  # 涓嶅瓨 active锛屼粎娴嬭瘯鍘嗗彶
+	var hist_slot := 2
+	var save_ok := SaveManager.save_game(hist_slot)
+	ok = ok and save_ok
+	var hist_path := "user://nk1_save_%d.json" % hist_slot
+	var file_content := FileAccess.get_file_as_string(hist_path)
+	var raw2: Dictionary = JSON.parse_string(file_content) if not file_content.is_empty() else {}
+	var we2: Dictionary = raw2.get("world_events", {})
+	ok = ok and we2.has("triggered_events")
+	ok = ok and we2.has("port_triggered")
+	ok = ok and we2.has("cooldowns")
+	ok = ok and we2["triggered_events"].get("pirate_attack", false)
+	ok = ok and we2.get("cooldowns", {}).has("quanzhou:pirate_attack")
+
+	# 鎵嬪姩浠庢枃浠跺姞杞?WE 鏁版嵁锛堥伩鍏?load_game 渚т綔鐢?+ 鍚庣画 load(0) 姹℃煋锛?+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+	var hist_content2: String = FileAccess.get_file_as_string(hist_path)
+	var hist_raw2: Dictionary = (JSON.parse_string(hist_content2) if not hist_content2.is_empty() else {}) as Dictionary
+	var hist_we2: Dictionary = hist_raw2.get("world_events", {}) as Dictionary
+	WorldEventTracker.from_save_dict(hist_we2)
+
+	ok = ok and WorldEventTracker.is_event_triggered("pirate_attack", "quanzhou")
+	ok = ok and not WorldEventTracker.can_trigger_event("pirate_attack", "quanzhou")
+	ok = ok and WorldEventTracker.is_on_cooldown("pirate_attack", "quanzhou")
+
+	# 杩囦竴澶╋紝鍐峰嵈搴旈€掑噺
+	WorldEventTracker.process_day()
+	ok = ok and WorldEventTracker.is_on_cooldown("pirate_attack", "quanzhou")
+
+	# 娓呯悊鎴戜滑浣跨敤鐨勬祴璇曟Ы浣嶏紝閬垮厤褰卞搷鍚庣画 all_saves 鏂█
+	ok = ok and SaveManager.delete_save(2)
+
+	# === NK1-P3-WORLDEVENT-005 max_triggers + 瀹夊叏闃€鍔犳潈娴嬭瘯 ===
+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+	WorldEventTracker.max_triggers_overrides.clear()
+
+	# 璁剧疆 trade_disaster 鏈€澶цЕ鍙?2 娆?+	WorldEventTracker.max_triggers_overrides["trade_disaster"] = 2
+
+	WorldEventTracker.record_trigger("trade_disaster", "quanzhou", 0)
+	ok = ok and WorldEventTracker.can_trigger_event("trade_disaster", "quanzhou")
+	WorldEventTracker.record_trigger("trade_disaster", "quanzhou", 0)
+	ok = ok and not WorldEventTracker.can_trigger_event("trade_disaster", "quanzhou")
+
+	# 鍏ㄥ眬 count 涔熺畻
+	WorldEventTracker.record_trigger("trade_disaster", "", 0)
+	# 鐜板湪 global count 楂橈紝浣?per port 涔?+
+	# 娓呯悊
+	WorldEventTracker.max_triggers_overrides.clear()
+
+	# === NK1-P3-WORLDEVENT-003 鏉冮噸鏈哄埗 + 閰嶇疆鍖栨祴璇?===
+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+	WorldEventTracker.active_events.clear()
+
+	# 楠岃瘉浜嬩欢閰嶇疆
+	var disaster_sample := TradeDisasterEvent.new("testport", 10)
+	ok = ok and disaster_sample.cooldown_days == 25
+	ok = ok and abs(disaster_sample.base_weight - 1.0) < 0.01
+	ok = ok and not disaster_sample.use_global_cooldown
+
+	var recovery_sample := TradeRecoveryEvent.new("testport", 10)
+	ok = ok and recovery_sample.cooldown_days == 15
+
+	# 璁剧疆瑙﹀彂鐘舵€?+	WorldEventTracker.record_trigger("trade_disaster", "quanzhou", 0)
+	WorldEventTracker.record_trigger("pirate_attack", "quanzhou", 5)
+
+	var cands := WorldEventTracker.get_weighted_event_candidates("quanzhou")
+	# 鎵惧埌瀵瑰簲
+	var d_w := 1.0
+	var r_w := 1.0
+	var p_w := 1.0
+	for c in cands:
+		if c.get("event_id", "") == "trade_disaster": d_w = c.get("weight", 1.0)
+		if c.get("event_id", "") == "trade_recovery": r_w = c.get("weight", 1.0)
+		if c.get("event_id", "") == "pirate_attack": p_w = c.get("weight", 1.0)
+
+	ok = ok and d_w < 0.1   # 宸茶Е鍙戝簲澶у箙闄嶄綆
+	ok = ok and p_w < 0.1   # 鍐峰嵈涓簲澶у箙闄嶄綆
+	ok = ok and r_w > 0.5   # recovery 鏈Е鍙戯紝搴斾繚鎸佽緝楂?+
+	# 娴嬭瘯 add_event 鎷夊彇浜嬩欢鑷韩 cooldown 閰嶇疆锛堜娇鐢?record 鍚庣殑鐘舵€侀獙璇侊級
+	WorldEventTracker.clear_trigger_history()
+	WorldEventTracker.clear_cooldowns()
+	var rec := TradeRecoveryEvent.new("quanzhou", 10)
+	WorldEventTracker.add_event(rec)  # 搴斾娇鐢?rec.cooldown_days = 15
+	ok = ok and WorldEventTracker.is_on_cooldown("trade_recovery", "quanzhou")
+
 	var info0: Dictionary = SaveManager.get_save_info(0)
 	ok = ok and info0.get("exists", false)
 	ok = ok and info0.get("balance", -1) == 2500
@@ -72,5 +224,18 @@ func _ready() -> void:
 	ok = ok and SaveManager.quick_save()
 	ok = ok and FileAccess.file_exists("user://nk1_save_0.json")
 
+	if not ok:
+		print("[SaveManagerSelfTest] FAIL - dumping WE state:")
+		print("  triggered_events=", WorldEventTracker.triggered_events)
+		print("  port_triggered=", WorldEventTracker.port_triggered)
+		print("  cooldowns=", WorldEventTracker.cooldowns)
+		print("  active count=", WorldEventTracker.get_active_events().size())
 	print("[SaveManagerSelfTest] %s" % ("PASS" if ok else "FAIL"))
-	get_tree().quit(0 if ok else 1)
\ No newline at end of file
+	get_tree().quit(0 if ok else 1)
+
+
+func _find_event(events: Array, event_id: String, target_port: String) -> BaseEconomicEvent:
+	for e in events:
+		if e != null and e.event_id == event_id and e.target_port == target_port:
+			return e
+	return null
