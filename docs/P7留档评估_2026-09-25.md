# 云端两套 P7 留档评估（2026-09-25，哈德）

对照对象：现在的 main（p6 四结局线 + 本地六历史结局线 + 旧事钩子 + 职衔五档 + 港卡三页）。

| 留档项 | 来源 | 现状对照 | 结论 |
|---|---|---|---|
| 港口节拍 `port_beats.json`（入港弹「有人找你」，按 order 播一条） | 21ce | 它要解决的是「序章断链、47 场不可达」。本地 main 已把主线修通（monk / merchant / dock / prepare / ryukyu_bay / hakata_ledger / return_quanzhou 全在开局链上），港上的追加剧情由云端 `chapters.json.story_hooks` + 酒馆「旧事」承担。剩下 50 场不可达的正是 21ce 自己标 `legacy` 的旧稿 | **不接**。机制重叠，只会多一套触发器 |
| 三结局 sea / scholar / both（按倾向差 ≥4 分流） | 21ce | p6 已有四结局按旗标分流 + 南海一纲兜底；本地又有六条历史结局。第三套没有位置 | **不接**，正文留档 |
| `endings.json` 数据驱动四结局、在指定港的住处选、多条并列玩家挑 | b05c | p6 的 `endings_at(port)` / `choose_ending(pick, port)` 已是「按港列出、玩家选一」；旗标条件也相近（south_sea / exam_road / ledger_open 与 p6 的 south_sea / sea_letter / ledger_distance 同源） | **不接**，数据留档作文案参考 |
| 声望阶 → `borrow_limit` 上限 3000/3800/4600/5400 | b05c | 云端职衔五档 `titles.json` 的 `loan_bonus` 0/500/1000/1800/2500 已覆盖同一件事 | **不接** |
| 年节旁白 1268 / 1276 各一句 | b05c | 本地 `news.json` 按月投放 23 条，含这两年的预告 | **不接** |
| **行会入行**：商誉 ≥ 8 且现钱 ≥ 2000 → 扣 2000、商誉 +4、人脉 +2、旗标 `guild_<港>`；只泉州 / 博多 / 广州；已入行只看账 | b05c | 现在行会只有「出港行情抄本」，商誉 / 人脉没有花钱口，`guild_*` 旗标无人用 | **可接**，半天：在 `_setup_guild` 加一张工席，旗标可作后续结局条件 |
| **贡院赴试**：每章一次，费 15 日；士人倾向占优则名声 +4、士人 +2、旗标 `exam_sat`；否则名声 +1、海路 +1 | b05c | 现在贡院只有「誊录三日换工钱」；`exam_sat` 旗标本地未用；15 日成本会错过一轮行情，是真代价 | **可接**，半天：在 `_setup_exam` 加第二张工席；本地 1268 殿试身份判定可顺手读 `exam_sat` |

建议：只接最后两项，其余留档不动。两项都改 Main.gd，须等 snowchan27-54 换皮收尾之后做，届时在新 main 上开分支、跑十二道门禁再合。

不做的理由一句话：三套结局机制（p6 四结局、本地六结局、云端三/四结局）不能并存，主线已选定 p6 + 本地；节拍与旁白是对同一问题的第二种解法。
