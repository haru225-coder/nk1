# 云端优先合并台账（align/cloud-first）

- 建账：2026-09-25（哈德）
- 拍板（Snow，2026-09-25）：**全面反转，云端为底**；两条海图分支都原样合，冲突哈德手解。
- 前情：09-24 的 `align/cursor-absorb` 走的是「以本地 main 为底、只挑云端 main 没有的改写吸收」，UI 线 85 提交与两套 P7 未收（见 `docs/云端分支吸收台账.md`）。本次反转后该分支保留不动，作为对照。
- 分支：`align/cloud-first`，工作树 `~/tmp/nk1-cloud-first`。起点 = 云端 UI 主干 `world-map-beauty-7f92`（d6fc623）。**未合 main、未 push。**

## 一、合入顺序与取舍规则

规则：① UI 壳按云端主干（最新）；② 云端对云端冲突「新的赢」，同一处多次重做取最新且功能是超集的一版；③ 追加型门禁段两侧并存（union）并重编号；④ 断言写门槛不写硬编码值；⑤ 本地 main 冲突让位云端、纯新增保留。

| 序 | 分支 | 处理 | 冲突要点 |
|---|---|---|---|
| 0 | world-map-beauty-7f92（85，含 p6 三条） | 起点 | — |
| 1 | fix-yamen-status-2b0f 叶子 | 主干赢 | 港名小匾是主干 `_place_chart_label` 的早期形态；门禁去掉 `_place_port_label` 一条 |
| 2 | seachart-ink-labels-5588 叶子 | **坞位一艘嵌进主干工席壳** | `_setup_shipyard` 两版重写：壳取工席，坞位/换船/待售改成三张工席 |
| 3 | inspection-patrol-b0e2 叶子 | 主干赢 + 巡检工具改写 | `patrol_shell.gd` 原量港口列表，改量三张航向牌（有窗口实测 82 项过） |
| 4 | ui-shell-refactor-c8fb 叶子 | 无代码冲突 | 设计文档章节重编号（十五坞位 / 十六海战顶匾 / 十七日志三卷） |
| 5 | setup-cloud-environment-91e9 | 干净 | — |
| 6–9 | be04 / 9d36 / c148 / 00b4（同一批审计修复四次重做） | 按 c148 逻辑（清旗舰货舱为超集）；场景 id 按 00b4 定名 `ryukyu_bay` / `hakata_ledger`；**发现调查取 be04/c148「计一日再点继续」**，00b4 的「不计日直接续航」未取（两票对一票且语义更准，偏离「新的赢」一次，已注明） | 门禁：city_inn 检查改认 `REMAPPED_FACILITIES`；`\A` 检查改认 `_unescape_scene_text` |
| 10 | morale-mutiny-7d9f | 原样 | `EventKind.MUTINY`；三工具 union |
| 11 | storm-hull-spread-7d9f | 原样 | 三工具 union |
| 12 | gameplay-course-and-contracts-bed9（9） | **航法/委办/传闻嵌进三向牌壳** | SeaChart 16 块：航法三策栏挂在航向牌上方；牌上加「遇事约 N 日　八成 N 日」与「·换风」；bed9 的 `_refresh_detail` 改写成船况面板文字段 `_course_detail_text`（八成/保货/受潮/委办赶不赶得上/交不齐）；委办提醒进歇息工席；`EventKind` 并 SHOAL/LOST |
| 13 | p7-chronicle-ending-design-b05c | **实现取主干 p6 结局系统**，`data/endings.json` 与设计文档留档 | 与主干 `ending_def/choose_ending/endings_at` 同名平行实现，不能共存 |
| 14 | p7-narrative-closure-brief-21ce | 同上；补进主干 `apply_effects` 缺的五键 `supplies/cargo/ship/cargo_loss/discovery`（+`_apply_cargo_loss`/`_discovery_id_for`） | 港口节拍 / 纪事结局未收；`verify_narrative.py`、`p7_smoke.gd` 留档不入门禁；verify_economy 终章检查改按主干 `endings` 列表 |
| 15 | real-sea-chart-2d51（2，含 09-24 折线航行） | **数据全收 + Voyage 折线里程/逐段罗经**；投影仍主干等比 0.78 | `_coast_data()` 用 Natural Earth 386 环替换手绘 15 环（[经,纬]→[纬,经]，按顶点数排序首环为大陆）、`chart_labels.json` 替换海名；当前航段沿 sealanes 折线画；`_sail_next_day` 每日 `Voyage.bearing_at`；bed9 三策 plan 用折线 `distance_li`（逐段风仍按 bed9 逐日风）；verify_economy 四条硬编码日数改关系式，比例检查改用直线大圆 |
| 16 | sea-chart-terrain-8b32（3，含 09-24） | 数据/生成脚本取 2d51 格式；`verify_coastline.py` 用 09-24 标准库版（按 8b32 检查项写、对 2d51 数据）并改接线检查 | 8b32 的绕陆格子路 / 港名排列由 2d51 sealanes + 主干 `_place_chart_label` 覆盖，未收第三套绘制；其 shapely 版门禁与取景常数段不收 |
| 17 | **本地 main（22）** | `merge -X ours` 后补回 25 个终局函数 + 常量并挂入口 | 见提交说明；删云端已拆的刷怪/旧列表/星号/AcceptDialog |

## 二、门禁状态（2026-09-25，`~/tmp/nk1-align/cloud-gates.sh verify_coastline check_assets verify_story_data simulate_endgame`）

| 门禁 | 结果 |
|---|---|
| check_symbols / verify_economy / simulate_run / verify_coastline | 全绿 |
| check_assets / verify_story_data（本地） | 全绿（各修一处判定：smoke 故意引用已拆图；`saved.get` 误当 `d.get`） |
| 编辑器扫描 | 0 错误（icon JPEG 假 PNG 已被 main 真 PNG 替换） |
| godot_smoke（云端） | PASS 139 项 |
| godot_compile_check（本地） | 17/17 |
| patrol_shell（有窗口） | PASS 82 项 |
| **simulate_endgame（本地）** | **3 项红**：四章跳年合计 / 最快 1264 年 / 第四章门槛——本地 P1 时间脊柱写在 chapters.json，已按云端 |
| **godot_story_check（本地）** | **45 项红**：陈文龙身份/新闻投放/殿试结算/存档 round-trip——依赖本地 scenes/chapters/剧情旗标，已按云端 |

## 三、待 Snow 拍板 / 点验

1. **两条红门禁的去向**：a) 把本地 P1 跳年门槛与终局剧情数据重接到云端 chapters.json / scenes.json（工作量中等，代码入口已全部挂好，只缺数据）；b) 或接受云端 p6 四结局线、把这两道门禁降级为参考。哈德倾向 a。
2. **真机点验**：海图（航法三策栏 + 三向牌 + 真实岸线 + 折线航迹 + 船况面板航段细节）、船屋坞位工席、兴化住处=玉湖陈宅、特殊卡在「今日只开三处」壳下是否可见（`_special_cards` 追加进 `_shore_facilities`，未确认发牌逻辑会否滤掉）。
3. 合回 main：Snow 点头后 `main` fast-forward 不了（分叉），需 `git merge align/cloud-first` 或直接把 main 指到该分支；仍不 push。
4. 保留对照：`align/cursor-absorb`（09-24 主干为底版）、`fusion/main-core-into-codex`。

## 四、环境备忘

- macOS 无 `timeout`，用 `perl -e 'alarm N; exec @ARGV' -- cmd`。
- user 数据目录 `~/Library/Application Support/Godot/app_userdata/nk1/saves` 跨副本共享；本地 story_check 用第 9 槽 round-trip，云端 smoke 断言第 9 槽为空——story_check 已加用完即删，gates 脚本跑 smoke 前也会挪开。
- Godot 门禁在 rsync 副本 `~/tmp/nk1-cloud-compile` 跑；跑前先 `--headless --editor --quit` 扫一遍让 class_name 注册。
